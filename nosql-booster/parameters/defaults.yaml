# -*- coding: utf-8 -*-
# vim: ft=yaml
#
# Set default values.
---
values:
  pkg:
    name: nosql-booster
  rootgroup: root
  config: '/etc/nosql-booster'
  service:
    name: nosql-booster
  subcomponent:
    config: '/etc/nosql-booster-subcomponent-formula.conf'
  # Just here for testing
  added_in_defaults: defaults_value
  winner: defaults
...
